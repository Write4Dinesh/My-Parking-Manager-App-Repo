<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
<import type="android.view.View"/>
        <variable
            name="parkingSpace"
            type="com.shrinvi.parkingapp.model.ParkingSpace" />
        <variable
            name="spaceItemViewModel"
            type="com.shrinvi.parkingapp.viewmodel.SpaceItemViewModel" />
    </data>


    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:background="@drawable/un_parked"
        android:onClick="@{()->spaceItemViewModel.onClick()}"
        android:orientation="horizontal"
        android:weightSum="2">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{parkingSpace.currentPosition}"
            android:layout_weight="1"
            android:layout_gravity="left"/>
        <ImageView
            android:id="@+id/image_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:scaleType="center"
            android:layout_weight="1"
            android:layout_gravity="right"
            android:src="@drawable/parked"
            android:visibility="@{parkingSpace.isEmpty?View.GONE:View.VISIBLE}" />
    </LinearLayout>
</layout>